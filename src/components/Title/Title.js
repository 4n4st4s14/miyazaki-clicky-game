import React from "react";
import "./Title.css";

const Title = props =>
<div className="header">
<h1 className="title"> Miyazaki Clicky Game!</h1>
<h3 className="desc">Click on an image to earn points, but don't click the same image twice!</h3>
</div>;

export default Title;
